import React from 'react'

const Searce = ({className, placeholder}) => {
  return (
    <div>
        <input className={className} placeholder={placeholder}></input>
    </div>
  )
}

export default Searce